{
  "divWithCssModuleClass": {
    "prefix": "dc",
    "body": ["<div className={styles.${1:className}}>${0}</div>"],
    "description": "div with CSS module className"
  },
  "typescriptReactFunctionalComponent": {
    "prefix": "tsrfc",
    "body": [
      "type Props = {}",
      "",
      "export default function ${TM_FILENAME_BASE}({}: Props) {",
      "  return (",
      "    <div>${0}</div>",
      "  )",
      "}"
    ],
    "description": "React Functional Component with TypeScript Props type"
  },
  "useEffect": {
    "prefix": "useEffectSnippet",
    "body": [
      "useEffect(() => {",
      "  ${1:setup}",
      "",
      "  return () => {",
      "    ${2:return}",
      "  }",
      "}, [${3:dependencies?}])",
      ""
    ]
  },
  "useMemo": {
    "prefix": "useMemoSnippet",
    "body": ["useMemo(() => ${1:first}, [${2:second}])"]
  },
  "useReducer": {
    "prefix": "useReducerSnippet",
    "body": [
      "const [state, dispatch] = useReducer(${1:first}, ${2:second}, ${3:third})"
    ]
  },
  "useRef": {
    "prefix": "useRefSnippet",
    "body": ["const ${1:first} = useRef(${2:second})"]
  },
  "useState": {
    "prefix": "useStateSnippet",
    "body": ["const [${1:state}, set${2:State}] = useState(${3:initValue})"]
  }
}
